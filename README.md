# mini-music
Simple mobile music player for children

## Local configuration

1. Copy `Mini Music/Config/DeveloperOverrides.xcconfig.example` to `Mini Music/Config/DeveloperOverrides.xcconfig` (this file stays local).
2. Set `APP_BUNDLE_IDENTIFIER` and `DEVELOPMENT_TEAM` to match your Apple Developer setup before building or archiving.
